#include "main.h"
/**
 * mul - Entry part
 * @a, @b: two numbers to mul.
 * Description: return c the value of mul axb
 *
 * Return: return c value
*/

int mul(int a, int b)
{
	int c = a * b;
	return (c);
}

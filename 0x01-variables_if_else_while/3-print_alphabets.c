#include <stdio.h>

/**
 * main - Entry part
 *
 * Description: print alphabet lowercase.
 *
 * Return: always 0 (success).
*/

int main(void)
{
	char c = 'a';

	while (c <= 'Z')
	{
		putchar(c);
		if (c == z)
		{
			c = 'A';
			continue;
		}
		c++;
	}

	putchar('\n');
	return (0);
}
